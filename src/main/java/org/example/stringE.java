//package org.example;
//
//public boolean stringE(String str) {
////   // Initialize a counter to 0
////   int count = 0;
//
////   // Loop through each character in the string
////   for (int i = 0; i < str.length(); i++) {
////     // If the character is 'e', increment the counter
////     if (str.charAt(i) == 'e') {
////       count++;
////     }
////   }
//
////   // Check if the counter is between 1 and 3
////   return count >= 1 && count <= 3;
//// }

/** We need to determine if a given string contains between 1 and 3 'e' characters.
 * If the number of 'e' characters is within this range, return true; otherwise, return false.
 * */
