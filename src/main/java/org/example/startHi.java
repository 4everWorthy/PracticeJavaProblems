//package org.example;
//
// public boolean startHi(String str) {
//   if(str.length() >= 2 && str.startsWith("hi")) {
//     return true;
//   } else {
//     return false;
//   }


/** Determine "if" a given string starts with "hi". "If" the string starts with "hi",
 * return true; otherwise, return false. First, we'll check "if" the "length"
 * of the string is at least 2 characters. Then, we'll use the "startsWith" method
 * to check if the string begins with "hi". Lastly, return true "else" return false.
 */